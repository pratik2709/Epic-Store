version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: DockerFile
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "80:8000"
    networks:
      - gameopedia
#    volumes:
#      - type: bind
#        source: /Users/pratiksaxena/projects/gameopedia
#        target: /code/db

networks:
  gameopedia:


